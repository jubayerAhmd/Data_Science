statistics
install.packages("corrplot")
install.packages("e1071")
install.packages("dplyr")
install.packages("ggplot2")
# ===== LIBRARIES =====
library(corrplot)
library(e1071)
library(dplyr)
library(ggplot2)
cat("All packages loaded successfully!\n")
#-------------------- Load Dataset -----------------------
# mydata <- read.csv("F:/semester-12/INTRODUCTION TO DATA SCIENCE/Project/My_Project_Mid/pdataset.csv")
mydata <- read.csv("https://drive.google.com/uc?export=download&id=15qCdIx9dZ--Kq0kO0NgEYywR77T-oGjb")
mydata <- mydata[1:1000, ]
# ------------------- Display the first 20 rows of the Dataset --------------------
head(mydata, 20)
# ------------------- Show shape (rows × columns) --------------------
dim(mydata)
# ------------------- Display data types of each column --------------------
# str(mydata)
sapply(mydata, class)
statistics <- data.frame(
Mean   = sapply(mydata, mean, na.rm = TRUE),
Median = sapply(mydata, median, na.rm = TRUE),
Mode   = sapply(mydata, get_mode),
SD     = sapply(mydata, sd, na.rm = TRUE),
Min    = sapply(mydata, min, na.rm = TRUE),
Max    = sapply(mydata, max, na.rm = TRUE),
Count  = sapply(mydata, function(x) sum(!is.na(x)))
)
statistics <- data.frame(
Mean   = sapply(mydata, mean, na.rm = TRUE),
Median = sapply(mydata, median, na.rm = TRUE),
# Mode   = sapply(mydata, get_mode),
SD     = sapply(mydata, sd, na.rm = TRUE),
Min    = sapply(mydata, min, na.rm = TRUE),
Max    = sapply(mydata, max, na.rm = TRUE),
Count  = sapply(mydata, function(x) sum(!is.na(x)))
)
statistics
get_mode <- function(x) {
ux <- unique(x)
ux[which.max(tabulate(match(x, ux)))]
}
statistics
statistics <- data.frame(
Mean   = sapply(mydata, mean, na.rm = TRUE),
Median = sapply(mydata, median, na.rm = TRUE),
Mode   = sapply(mydata, get_mode),
SD     = sapply(mydata, sd, na.rm = TRUE),
Min    = sapply(mydata, min, na.rm = TRUE),
Max    = sapply(mydata, max, na.rm = TRUE),
Count  = sapply(mydata, function(x) sum(!is.na(x)))
)
statistics
statistics <- data.frame(
cat("---------Mean--------\n"),
Mean   = sapply(mydata, mean, na.rm = TRUE),
Median = sapply(mydata, median, na.rm = TRUE),
Mode   = sapply(mydata, get_mode),
SD     = sapply(mydata, sd, na.rm = TRUE),
Min    = sapply(mydata, min, na.rm = TRUE),
Max    = sapply(mydata, max, na.rm = TRUE),
Count  = sapply(mydata, function(x) sum(!is.na(x)))
)
cat("---------Mean--------\n"+ 56 )
cat("---------Mean--------\n"+ string(56) )
cat("---------Mean--------\n")
Mean_values <- sapply(mydata, mean, na.rm = TRUE)
# str(mydata)
sapply(mydata, class)
get_mode <- function(x) {
ux <- unique(x)
ux[which.max(tabulate(match(x, ux)))]
}
cat("---------Mean--------\n")
Mean_values <- sapply(mydata, mean, na.rm = TRUE)
cat("---------Median--------\n")
Median_values <- sapply(mydata, median, na.rm = TRUE)
cat("---------Mode--------\n")
Mode_values <- sapply(mydata, get_mode)
cat("---------SD(Standard Deviation)--------\n")
SD_values   <- sapply(mydata, sd, na.rm = TRUE)
cat("---------Min--------\n")
Min_values  <- sapply(mydata, min, na.rm = TRUE)
cat("---------Max--------\n")
Max_values  <- sapply(mydata, max, na.rm = TRUE)
cat("---------Count--------\n")
Count_values<- sapply(mydata, function(x) sum(!is.na(x)))
statistics <- data.frame(
Mean   = Mean_values,
Median = Median_values,
Mode   = Mode_values,
SD     = SD_values,
Min    = Min_values,
Max    = Max_values,
Count  = Count_values
)
statistics
statistics <- data.frame(
Mean   = Mean_values,
Median = Median_values,
Mode   = Mode_values,
SD     = SD_values,
Min    = Min_values,
Max    = Max_values,
Count  = Count_values
)
cat("---------Mean--------\n")
Mean_values <- sapply(mydata, mean, na.rm = TRUE)
cat("---------Median--------\n")
Median_values <- sapply(mydata, median, na.rm = TRUE)
cat("---------Mode--------\n")
Mode_values <- sapply(mydata, get_mode)
cat("---------SD(Standard Deviation)--------\n")
SD_values   <- sapply(mydata, sd, na.rm = TRUE)
cat("---------Min--------\n")
Min_values  <- sapply(mydata, min, na.rm = TRUE)
cat("---------Max--------\n")
Max_values  <- sapply(mydata, max, na.rm = TRUE)
cat("---------Count--------\n")
Count_values<- sapply(mydata, function(x) sum(!is.na(x)))
statistics <- data.frame(
Mean   = Mean_values,
Median = Median_values,
Mode   = Mode_values,
SD     = SD_values,
Min    = Min_values,
Max    = Max_values,
Count  = Count_values
)
statistics
get_mode <- function(x) {
ux <- unique(x)
ux[which.max(tabulate(match(x, ux)))]
}
statistics <- data.frame(
Mean   = sapply(mydata, mean, na.rm = TRUE),
Median = sapply(mydata, median, na.rm = TRUE),
Mode   = sapply(mydata, get_mode),
SD     = sapply(mydata, sd, na.rm = TRUE),
Min    = sapply(mydata, min, na.rm = TRUE),
Max    = sapply(mydata, max, na.rm = TRUE),
Count  = sapply(mydata, function(x) sum(!is.na(x)))
)
statistics
statistics <- data.frame(
get_mode <- function(x) {
ux <- unique(x)
ux[which.max(tabulate(match(x, ux)))]
}
Mean   = sapply(mydata, mean, na.rm = TRUE),
statistics <- data.frame(
get_mode <- function(x) {
ux <- unique(x)
ux[which.max(tabulate(match(x, ux)))]
},
Mean   = sapply(mydata, mean, na.rm = TRUE),
Median = sapply(mydata, median, na.rm = TRUE),
Mode   = sapply(mydata, get_mode),
SD     = sapply(mydata, sd, na.rm = TRUE),
Min    = sapply(mydata, min, na.rm = TRUE),
Max    = sapply(mydata, max, na.rm = TRUE),
Count  = sapply(mydata, function(x) sum(!is.na(x)))
)
statistics <- data.frame(
"get_mode <- function(x) {
ux <- unique(x)
ux[which.max(tabulate(match(x, ux)))]
}",
Mean   = sapply(mydata, mean, na.rm = TRUE),
Median = sapply(mydata, median, na.rm = TRUE),
Mode   = sapply(mydata, get_mode),
SD     = sapply(mydata, sd, na.rm = TRUE),
Min    = sapply(mydata, min, na.rm = TRUE),
Max    = sapply(mydata, max, na.rm = TRUE),
Count  = sapply(mydata, function(x) sum(!is.na(x)))
)
statistics
statistics
get_mode <- function(x) {
ux <- unique(x)
ux[which.max(tabulate(match(x, ux)))]
}
statistics <- data.frame(
Mean   = sapply(mydata, mean, na.rm = TRUE),
Median = sapply(mydata, median, na.rm = TRUE),
Mode   = sapply(mydata, get_mode),
SD     = sapply(mydata, sd, na.rm = TRUE),
Min    = sapply(mydata, min, na.rm = TRUE),
Max    = sapply(mydata, max, na.rm = TRUE),
Count  = sapply(mydata, function(x) sum(!is.na(x)))
)
statistics
numeric_features <- names(mydata)[sapply(mydata, is.numeric)]
numeric_features
numeric_features <- names(mydata)[sapply(mydata, is.numeric)]
numeric_features
outlier_summary <- data.frame()
for (col in numeric_features) {
Q1 <- quantile(mydata[[col]], 0.25, na.rm = TRUE)
Q3 <- quantile(mydata[[col]], 0.75, na.rm = TRUE)
IQR_val <- Q3 - Q1
lower_bound <- Q1 - 1.5 * IQR_val
upper_bound <- Q3 + 1.5 * IQR_val
outliers <- sum(mydata[[col]] < lower_bound | mydata[[col]] > upper_bound)
outlier_summary <- rbind(outlier_summary,data.frame(Feature=col, Outliers=outliers))
}
outlier_summary
